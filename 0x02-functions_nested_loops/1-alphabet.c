#include <stdio.h>
#include "main.h"
/**
 *main - Entry point
 *
 * Return: Always 0 (success)
 */
void print_alphabet(void)
{
int alph;
for (alph = 'a'; alpha <= 'z'; alpha++)
	_putchar(alpha);
_putchar('\n');
return (0);
}
